spring:
  application:
    name: finance
  profiles:
    active: dev

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  flyway:
    enabled: false

  data:
    redis:
      host: localhost
      port: 6379
      # password: caso seu redis tenha senha
      connect-timeout: 2000ms
      timeout: 1000ms